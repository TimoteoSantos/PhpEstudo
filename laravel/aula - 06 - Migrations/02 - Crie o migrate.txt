php artisan make:migration Produto

<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        //criar um metodo que cria o migration passando como parametro uma instacia
        //Blueprint com uma variavel chamada de $table
        Schema::create('produtos', function(Blueprint $table){

            $table->id();
            $table->string('nome');
            $table->text('descricao');
            $table->double('preco', 10,2);
            $table->string('slug');
            $table->string('imagem');
            
            //primeiro criar o campo que recebera o relacionamento
            $table->unsignedBigInteger('id_user');
            //agora devemos configurar o relacionamento
            $table->foreign('id_user')->references('id')->on('users')->onDelete()->onUpdate('cascade');

            //configurar outro relacionamento
            $table->unsignedBigInteger('id_categoria');
            $table->foreign('id_categoria')->references('id')->on('categoria')->onDelete()->onUpdate('cascade');
            
            $table->timestamp();

        });
        
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        //
    }
};
